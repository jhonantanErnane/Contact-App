import 'package:flutter_test/flutter_test.dart';

import 'package:contact_app/app/shared/services/sync_log_service.dart';

void main() {
  SyncLogService service;

  setUp(() {
//    service = SyncLogService();
  });
//
  group('SyncLogService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<SyncLogService>());
//    });
//
  });
}
