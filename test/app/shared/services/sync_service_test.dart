import 'package:flutter_test/flutter_test.dart';

import 'package:contact_app/app/shared/services/sync_service.dart';

void main() {
  SyncService service;

  setUp(() {
//    service = SyncService();
  });
//
  group('SyncService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<SyncService>());
//    });
//
  });
}
