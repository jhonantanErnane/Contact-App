import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:contact_app/app/shared/widgets/sync_progress/sync_progress_controller.dart';
import 'package:contact_app/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SyncProgressController syncprogress;
  //
  setUp(() {
    //     syncprogress = AppModule.to.get<SyncProgressController>();
  });

  group('SyncProgressController Test', () {
    //   test("First Test", () {
    //     expect(syncprogress, isInstanceOf<SyncProgressController>());
    //   });

    //   test("Set Value", () {
    //     expect(syncprogress.value, equals(0));
    //     syncprogress.increment();
    //     expect(syncprogress.value, equals(1));
    //   });
  });
}
