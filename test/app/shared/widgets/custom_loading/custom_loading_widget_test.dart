import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:contact_app/app/shared/widgets/custom_loading/custom_loading_widget.dart';

main() {
//  testWidgets('CustomLoadingWidget has message', (WidgetTester tester) async {
//    await tester.pumpWidget(buildTestableWidget(CustomLoadingWidget()));
//    final textFinder = find.text('CustomLoading');
//    expect(textFinder, findsOneWidget);
//  });
}
