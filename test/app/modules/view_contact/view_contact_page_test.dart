import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:contact_app/app/modules/view_contact/view_contact_page.dart';

main() {
  testWidgets('ViewContactPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ViewContactPage(title: 'ViewContact')));
    //  final titleFinder = find.text('ViewContact');
    //  expect(titleFinder, findsOneWidget);
  });
}
