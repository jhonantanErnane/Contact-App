import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:contact_app/app/modules/view_contact/view_contact_controller.dart';
import 'package:contact_app/app/modules/view_contact/view_contact_module.dart';

void main() {
  initModule(ViewContactModule());
  // ViewContactController viewcontact;
  //
  setUp(() {
    //     viewcontact = ViewContactModule.to.get<ViewContactController>();
  });

  group('ViewContactController Test', () {
    //   test("First Test", () {
    //     expect(viewcontact, isInstanceOf<ViewContactController>());
    //   });

    //   test("Set Value", () {
    //     expect(viewcontact.value, equals(0));
    //     viewcontact.increment();
    //     expect(viewcontact.value, equals(1));
    //   });
  });
}
