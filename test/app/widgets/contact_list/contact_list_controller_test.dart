import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:contact_app/app/widgets/contact_list/contact_list_controller.dart';
import 'package:contact_app/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ContactListController contactlist;
  //
  setUp(() {
    //     contactlist = AppModule.to.get<ContactListController>();
  });

  group('ContactListController Test', () {
    //   test("First Test", () {
    //     expect(contactlist, isInstanceOf<ContactListController>());
    //   });

    //   test("Set Value", () {
    //     expect(contactlist.value, equals(0));
    //     contactlist.increment();
    //     expect(contactlist.value, equals(1));
    //   });
  });
}
